# The public URL of the Render API service. Do not add a trailing slash.
# Leave unset for local development: Vite proxies /api to localhost:3000.
VITE_API_URL=https://your-render-service.onrender.com
